outfile = open('texting.txt','w') # opening a file for writing by creating a file object named outfile
outfile.write('This is to test a file by writing to it') # using the write() method to write on a file
outfile.close() # using the close() method to close out the file, otherwise it says open.
